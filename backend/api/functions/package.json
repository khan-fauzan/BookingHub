{
  "name": "hotel-booking-lambda-functions",
  "version": "1.0.0",
  "description": "Lambda functions for Hotel Booking Platform API",
  "main": "index.js",
  "scripts": {
    "install-all": "for dir in */; do (cd \"$dir\" && npm install); done",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "hotel",
    "booking",
    "lambda",
    "aws"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@aws-sdk/client-dynamodb": "^3.700.0",
    "@aws-sdk/lib-dynamodb": "^3.700.0",
    "@aws-sdk/util-dynamodb": "^3.700.0",
    "uuid": "^11.0.3"
  },
  "devDependencies": {
    "@types/aws-lambda": "^8.10.145",
    "@types/node": "^22.10.2"
  }
}
